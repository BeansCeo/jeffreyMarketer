name: Access Secret Variable

on:
  push:
    branches:
      - main

jobs:
  access-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Access Secret
        env:
          MY_SECRET: ${{ secrets.MESSAGE }}
        run: |
          echo "$MY_SECRET"
