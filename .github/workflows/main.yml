name: Access Secret Variable

on:
  push:
    branches:
      - main

jobs:
  access-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Access Secret
        env:
          KEY: ${{ secrets.APIKEY }}
        run: |
          echo "$KEY"
